<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                    android:id="@+id/playerContainer"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical">

    <!-- Search Bar Container -->
    <LinearLayout   android:layout_width="match_parent"
                    android:layout_height="40dp"
                    android:orientation="horizontal"
                    android:id="@+id/search_bar_container"
                    android:background="@color/grey">

        <!-- Search Dialog -->
        <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/edit_text_sound_search"
                android:layout_weight="3"
                android:hint="@string/sound_search_suggestion"/>

        <!-- Search Button -->
        <ImageButton
                android:contentDescription="@string/search_content_description"
                android:layout_width="60dp"
                android:layout_height="match_parent"
                android:id="@+id/btn_sound_search"
                android:src="@drawable/ic_action_search"/>

    </LinearLayout>

    <!-- Player Header -->
    <LinearLayout
            android:id="@+id/player_header_bg"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        <!-- Song Title -->
        <TextView
                android:id="@+id/song_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#04b3d2"
                android:textSize="16sp"
                android:textStyle="bold"
                android:text="The Good, The Bad And The Ugly"/>
    </LinearLayout>

<LinearLayout
                android:id="@+id/thumbnail_equalizer_container"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:name="it.polimi.dima.sound4u.ThumbnailFragment">
</LinearLayout>

    <!-- Player Buttons Container -->
    <LinearLayout android:layout_width="fill_parent"
                  android:layout_height="wrap_content"
                  android:orientation="horizontal">
        <!-- Play Button -->
        <Button
                style="?android:attr/buttonStyleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Play"
                android:id="@+id/btn_play"/>
        <!-- Play Button -->
        <Button
                style="?android:attr/buttonStyleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pause"
                android:id="@+id/btn_pause"/>
        <!-- Play Button -->
        <Button
                style="?android:attr/buttonStyleSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Stop"
                android:id="@+id/btn_stop"/>
    </LinearLayout>
    <!-- Player Footer -->
    <LinearLayout
            android:id="@+id/player_footer_bg"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical">

        <!-- Progress Bar/Seek bar -->
        <SeekBar
                android:id="@+id/seek_bar"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:thumb="@drawable/seek_handler"
                android:progressDrawable="@drawable/img_seekbar_progress_blue"
                android:paddingLeft="30dp"
                android:paddingRight="30dp"/>

        <!-- Timer Display -->
        <LinearLayout
                android:id="@+id/timer_display"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_marginRight="20dp"
                android:layout_marginLeft="20dp"
                android:layout_marginBottom="10dp">
            <!-- Current Duration Label -->
            <TextView
                    android:id="@+id/current_duration_label"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="left"
                    android:textColor="#eeeeee"
                    android:textStyle="bold"/>
            <!-- Total Duration Label -->
            <TextView
                    android:id="@+id/total_duration_label"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="right"
                    android:textColor="#04cbde"
                    android:textStyle="bold"/>
        </LinearLayout>
        <!-- Download / Equalizer buttons -->
        <LinearLayout
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:gravity="center">
            <!-- Download Button -->
            <Button
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/download_button"
                    android:id="@+id/btn_download"/>
            <!-- Equalizer Button -->
            <Button
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/equalizer_button"
                    android:id="@+id/btn_equalizer"
                    android:onClick="showEqualizer"/>
            <Button
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/btn_send"
                    android:id="@+id/btn_send"/>
        </LinearLayout>
    </LinearLayout>




</LinearLayout>